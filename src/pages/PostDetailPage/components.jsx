import styled from '@emotion/styled';

export const NavButton = styled.button`
  padding: 12px;
  border-radius: 6px;
  display: inline-flex;
  align-items: center;
  gap: 4px;

  span {
    font-size: 14px;
    color: rgba(0, 0, 0, 0.8);
  }
`;
